catalog: instances
id: ~
admin_mail: admin@example.net
services:
    mailserver:
        enabled: yes
        hostname: example.net
    webserver:
        enabled: yes
    dnsserver:
        enabled: yes
    certificates:
        enabled: yes
